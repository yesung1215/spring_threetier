<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.app.threetier.mapper.PostMapper">

<!--  1. 게시글 등록 -->
    <insert id="insert" parameterType="PostVO">
        <selectKey keyProperty="id" resultType="Long" order="BEFORE">
            SELECT SEQ_POST.NEXTVAL FROM DUAL
        </selectKey>
        INSERT INTO TBL_POST(ID, POST_TITLE, POST_CONTENT, MEMBER_ID)
        VALUES(#{id}, #{postTitle}, #{postContent}, #{memberId})
    </insert>

<!--    2. 게시글 목록(JOIN - 작성자 이름, 작성자 이메일)-->
    <select id="selectAll" resultType="PostDTO">
        SELECT TBP.ID, TBB.MEMBER_NAME, TBB.MEMBER_EMAIL, TBP.POST_TITLE, TBP.POST_READ_COUNT
        FROM TBL_POST TBP
        JOIN TBL_MEMBER TBB
        ON TBP.MEMBER_ID = TBB.ID
        ORDER BY TBP.ID DESC
    </select>

<!--    3. 게시글 상세 조회(JOIN - 작성자 이름, 작성자 이메일)-->
    <select id="select" parameterType="Long" resultType="PostDTO">
        SELECT TBP.ID, TBB.MEMBER_NAME, TBB.MEMBER_EMAIL, TBP.POST_TITLE, TBP.POST_CONTENT ,TBP.POST_READ_COUNT
        FROM TBL_POST TBP
        JOIN TBL_MEMBER TBB
        ON TBP.MEMBER_ID = TBB.ID
        WHERE TBP.ID = #{id}
    </select>

<!--    3-1 조회수 +1-->
    <update id="updateReadCount" parameterType="Long">
        UPDATE TBL_POST
        SET POST_READ_COUNT = POST_READ_COUNT + 1
        WHERE ID = #{id}
    </update>


<!--    4. 게시글 수정-->
    <update id="update" parameterType="PostVO">
        UPDATE TBL_POST
        SET POST_TITLE = #{postTitle}, POST_CONTENT = #{postContent}
        WHERE ID = #{id}
    </update>

<!--    5. 게시글 삭제-->
    <delete id="delete" parameterType="Long">
        DELETE FROM TBL_POST
        WHERE ID = #{id}
    </delete>

</mapper>